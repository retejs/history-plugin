{
  "name": "rete-history-plugin",
  "version": "0.1.0",
  "description": "",
  "main": "build/history-plugin.min.js",
  "module": "build/{{bundleName}}.debug.js",
  "scripts": {
    "build": "rete --build rete.config.js",
    "build:dev": "rete --build rete.config.js --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "rete-cli": "^0.3.0"
  }
}
